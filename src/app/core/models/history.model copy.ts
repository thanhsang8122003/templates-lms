
export interface IHistory {
    id?: string,
    uid_user: string,
    topic: number,
    reading: number,
    listening: number,
    created_at: string
    total: number,
    range_score: string,
    month: string,
    name: string,
}